apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx
  namespace: demo
spec:
  parentRefs:
  - name: my-gateway
    namespace: default
  hostnames:
  - "nginxservice1.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: nginx1service
      kind: Service
      port: 80
  # hostnames:
  # - "nginxservice2.com"
  # rules:
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /
  #   backendRefs:
  #   - name: nginx2service
  #     port: 80
  # hostnames:
  # - "nginxservice3.com"
  # rules:
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /
  #   backendRefs:
  #   - name: nginx3service
  #     kind: Service
  #     port: 80